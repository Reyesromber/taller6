package dominio;

/**
 * La clase Estado de tipo enum representa los posibles estados por los que
 * puede pasar una incidencia.
 * 
 * @author reyes
 */
public enum Estado {
	PENDIENTE, ELIMINADA, RESUELTA
}
